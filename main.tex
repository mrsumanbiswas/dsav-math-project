% main.tex
\documentclass[12pt, a4paper]{book}

% Include the font setup
\usepackage{font-setup}

% Include the color setup
\usepackage{color-setup}

% Include the layout setup
\usepackage{layout-setup}

% Include the hyperlinks setup
\usepackage{hyperlinks-setup}

% Include the chapters setup
\usepackage{chapters-setup}

% Include the standards setup
\usepackage{standards-setup}

% Include the theme setup
\usepackage{theme-setup}

\begin{document}

\frontmatter
% ... (rest of your document)

\end{document}
